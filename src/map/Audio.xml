<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.ssm.mapper.AudioMapper">
	<sql id="audioAllColumns">id,userId,audioUrl,audioText</sql>
	<sql id="audioColumns">userId,audioUrl,audioText</sql>
	<resultMap type="Audio" id="userResultMap">
		<id column="id" property="id"/>
		<result column="userId" property="userId"/>
		<result column="audioUrl" property="audioUrl"/>
		<result column="audioText" property="audioText"/>
	</resultMap>
	<insert id="addAudio" parameterType="Audio" useGeneratedKeys="true" keyProperty="id">
		insert t_user (<include refid="audioColumns"/>) values (#{userId},#{audioUrl},#{audioText})
	</insert>
	
	<select id="findAudioByUserId" parameterType="long" resultMap="audioResultMap">
		select * from t_audio where userId = #{userId}
	</select>
	
	<update id="updateAudio" parameterType="Audio">
		update t_audio set 1=1
		<if test="userId != null">
			, userId = #{userId}
		</if>
		<if test="audioInfo != null">
			and audioUrl = #{audioUrl}
		</if>
		<if test="version > 0">
			and audioText = #{audioText}
		</if>
		where id = #{id} 
	</update>
	
	<delete id="deleteAudioByUserId" parameterType="long">
		 delete from t_audio where userId=#{userId} 
	</delete>
	
	<delete id="deleteAudioById" parameterType="long">
		 delete from t_audio where id=#{id} 
	</delete>
</mapper>